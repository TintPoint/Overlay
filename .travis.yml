language: swift
osx_image: xcode9.3beta
branches:
  only:
    - master
env:
  global:
    - SDK="iphonesimulator"
  matrix:
    - DESTINATION="OS=9.0,name=iPhone 6s" MODE="test" CONFIGURATION="Debug" SCHEME="Overlay"
    - DESTINATION="OS=10.0,name=iPhone 7" MODE="test" CONFIGURATION="Debug" SCHEME="Overlay"
    - DESTINATION="OS=11.0,name=iPhone 7" MODE="test" CONFIGURATION="Debug" SCHEME="Overlay"
    - DESTINATION="OS=11.0,name=iPad Pro (10.5-inch)" MODE="test" CONFIGURATION="Debug" SCHEME="Overlay"
    - DESTINATION="OS=11.0,name=iPhone 7" MODE="build" CONFIGURATION="Release" SCHEME="Overlay"
    - DESTINATION="OS=11.0,name=iPad Pro (10.5-inch)" MODE="build" CONFIGURATION="Release" SCHEME="Overlay"
script:
  - set -o pipefail
  - xcodebuild "$MODE" -scheme "$SCHEME" -sdk "$SDK" -destination "$DESTINATION" -configuration "$CONFIGURATION" | xcpretty
